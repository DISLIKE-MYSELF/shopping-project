# Frontend Module (Vue.js)

## Build and Run
1. Install dependencies: 
npm install
2. Build production bundle: 
npm run build
3. Build Docker image: docker build -t shop_frontend -f Dockerfile.frontend .

